rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read and write access to any file, only if the user is authenticated
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
